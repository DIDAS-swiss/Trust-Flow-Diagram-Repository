<!--
Adding or changing a flow? Fill in the classification block below. It is two
lines and it stops the repository acquiring four names for one sector.

Everything else in this template is optional. Delete what does not apply.
-->

## What this changes

<!-- One or two sentences. What does the diagram now show that it did not? -->

## Classification

<!--
Skip this section for changes that touch no flow: workflows, styling, prose.

Look both up rather than typing them from memory - the section letters moved
between NOGA 2008 and NOGA 2025, and a function that sounds right is often
already in the list under a different name:

    python3 scripts/check-classification.py --noga <search>
    python3 scripts/check-classification.py --functions <search>

New sector? `--new <sector> <division>` writes the sector.yaml for you.

Prefer to pick from a list? Open a "Propose a new flow" issue instead: its
dropdowns are generated from the same catalogues, so they cannot offer you
anything the check will reject.
-->

- **Sector directory:**
- **NOGA division:**
- **Family:**
- **Primary function:**

- [ ] `sector.yaml` updated, or the flow joins a family that already exists
- [ ] `python3 scripts/check-classification.py` passes

## Anything a reviewer should know

<!--
Uncertain about the division or the function? Say so here rather than guessing
quietly - picking the wrong one is easy to fix in review and expensive to fix
after other flows have copied it.
-->
